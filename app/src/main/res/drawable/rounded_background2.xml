<!-- res/drawable/rounded_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="?attr/primaryBackground"/> <!-- Color de fondo -->
    <corners android:radius="16dp"/> <!-- Radio de las esquinas redondeadas -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
    <stroke
        android:width="1dp"
        android:color="?attr/secondaryTextColor"/> <!-- Borde opcional -->
</shape>
